
@Library('bacsystem-shared-lib') _

def params = [
                        process   : "Gitflow",
                        dsl     : this,
                        wout    : true,
                        smc     : true,
                        opt     : false
                    ]
echo "params ${params}"
process(params)